apiVersion: v1
kind: Service
metadata:
  name: abonnement-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: abonnement-service
  ports:
    - name: http
      port: 8083
      targetPort: http

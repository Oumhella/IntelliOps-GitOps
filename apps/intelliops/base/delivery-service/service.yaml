apiVersion: v1
kind: Service
metadata:
  name: delivery-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: delivery-service
  ports:
    - name: http
      port: 8087
      targetPort: http

apiVersion: v1
kind: Service
metadata:
  name: lead-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lead-service
  ports:
    - name: http
      port: 8082
      targetPort: http

apiVersion: v1
kind: Service
metadata:
  name: paiement-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: paiement-service
  ports:
    - name: http
      port: 8086
      targetPort: http
